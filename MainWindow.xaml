<Window x:Class="FocusDeck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="FocusDeck"
        Height="700"
        Width="900"
        MinHeight="540"
        MinWidth="720"
        Background="#191919"
        WindowStyle="None"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="9" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#2B2B2B" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#383838" />
        <SolidColorBrush x:Key="InputBrush" Color="#343434" />
        <SolidColorBrush x:Key="TextBrush" Color="#F0F0F2" />
        <SolidColorBrush x:Key="MutedBrush" Color="#BDBDC2" />

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#F0F0F2" />
            <Setter Property="Background" Value="#353535" />
            <Setter Property="BorderBrush" Value="#444444" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="MinHeight" Value="42" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#404040" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#555555" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2D2D2D" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="#07121C" />
            <Setter Property="Background" Value="#43A6F3" />
            <Setter Property="BorderBrush" Value="#43A6F3" />
        </Style>

        <Style x:Key="ChromeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="32" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="#E7E7E9" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#303030" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#3A3A3A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#F2F2F4" />
            <Setter Property="Background" Value="#383838" />
            <Setter Property="BorderBrush" Value="#424242" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="CaretBrush" Value="#F2F2F4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#4BA7EE" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Width" Value="108" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" Background="#242424" BorderBrush="#3A3A3A" BorderThickness="1" CornerRadius="22" />
                            <TextBlock x:Name="StateText" Text="已关闭" Foreground="#A8A8AD" FontSize="14" VerticalAlignment="Center" Margin="16,0,0,0" />
                            <Border x:Name="Thumb" Width="28" Height="28" CornerRadius="14" Background="#D9D9DB" HorizontalAlignment="Right" Margin="0,0,7,0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#2389D3" />
                                <Setter TargetName="Track" Property="BorderBrush" Value="#2490DF" />
                                <Setter TargetName="StateText" Property="Text" Value="已开启" />
                                <Setter TargetName="StateText" Property="Foreground" Value="#FFFFFF" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Left" />
                                <Setter TargetName="Thumb" Property="Margin" Value="73,0,0,0" />
                                <Setter TargetName="Thumb" Property="Background" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="BorderBrush" Value="#5A5A5A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#F2F2F4" />
            <Setter Property="Background" Value="#383838" />
            <Setter Property="BorderBrush" Value="#424242" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Style="{x:Null}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <Border x:Name="Root"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                    <Grid>
                                        <ContentPresenter x:Name="ContentSite"
                                                          Margin="{TemplateBinding Padding}"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" />
                                        <TextBox x:Name="PART_EditableTextBox"
                                                 Style="{x:Null}"
                                                 Visibility="Hidden"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Foreground="#F2F2F4"
                                                 CaretBrush="#F2F2F4"
                                                 Margin="{TemplateBinding Padding}"
                                                 VerticalContentAlignment="Center" />
                                        <Path x:Name="Arrow"
                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="#DADADF"
                                              Width="8"
                                              Height="4"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Margin="0,0,14,0" />
                                    </Grid>
                                </Border>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#2F2F2F"
                                        BorderBrush="#4BA7EE"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="280">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#4BA7EE" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#5A5A5A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#F2F2F4" />
            <Setter Property="Background" Value="#2F2F2F" />
            <Setter Property="Padding" Value="12,9" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#3E6F93" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2D83C5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#191919">
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <Grid Background="#191919" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                <Border Width="25" Height="17" Background="#2F95D1" CornerRadius="1" Margin="0,0,14,0" />
                <TextBlock Text="FocusDeck" FontSize="19" Foreground="#F0F0F2" VerticalAlignment="Center" Margin="0,0,14,0" />
                <TextBlock Text="程序快捷唤起" FontSize="14" Foreground="#A9A9AD" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,12,12,0" VerticalAlignment="Top" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource ChromeButtonStyle}" Content="─" Click="Minimize_Click" />
                <Button Style="{StaticResource ChromeButtonStyle}" Content="□" Click="Maximize_Click" />
                <Button Style="{StaticResource ChromeButtonStyle}" Content="×" Click="Close_Click" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" Background="#202020" Padding="30,26,30,26" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Height="184" CornerRadius="12" ClipToBounds="True" Background="#151818" BorderBrush="#353535" BorderThickness="1" Margin="0,0,0,22">
                    <Grid>
                        <Image Source="Assets/cover.jpg" Stretch="UniformToFill" Opacity="0.45" />
                        <Rectangle Fill="#DD111212" />
                        <StackPanel Margin="26,0,0,24" VerticalAlignment="Bottom">
                            <TextBlock Text="FocusDeck" Foreground="#FFFFFF" FontSize="29" FontWeight="SemiBold" />
                            <TextBlock Text="一键唤起你的工作窗口" Foreground="#D6D3C8" FontSize="15" Margin="0,7,0,0" />
                        </StackPanel>
                        <Image Source="Assets/cover.jpg" Width="184" Height="184" Stretch="Uniform" HorizontalAlignment="Right" Margin="0,0,34,0" />
                    </Grid>
                </Border>

                <Border Background="#2B2B2B" BorderBrush="#353535" BorderThickness="1" CornerRadius="9" Padding="27,22" Margin="0,0,0,28">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="开机自动启动" Foreground="#F0F0F2" FontSize="20" FontWeight="SemiBold" />
                            <TextBlock Text="Windows 登录后在后台启动，可随时使用配置的快捷键。" Foreground="#BDBDC2" FontSize="15" Margin="0,7,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="StartupStateText" Text="已关闭" Foreground="#A8A8AD" VerticalAlignment="Center" Margin="0,0,12,0" />
                            <ToggleButton x:Name="StartupToggle" Width="72" Height="34" Content="" Checked="StartupToggle_Changed" Unchecked="StartupToggle_Changed" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="程序唤起快捷键" Foreground="#F0F0F2" FontSize="23" FontWeight="SemiBold" />
                        <TextBlock Text="按下快捷键切换到程序窗口；窗口已在前台时再次按下会最小化。" Foreground="#BDBDC2" FontSize="15" Margin="0,7,0,0" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="添加程序" Click="AddApplication_Click" Style="{StaticResource PrimaryButtonStyle}" />
                </Grid>

                <StackPanel x:Name="ApplicationsPanel" />
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Background="#1D1D1D">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="NoticeText" VerticalAlignment="Center" Foreground="#AEECC6" Margin="28,0,0,0" />
            <Button Grid.Column="1" Content="取消修改" Click="Reset_Click" Margin="0,0,12,0" Padding="18,10" />
            <Button Grid.Column="2" Content="保存设置" Click="Save_Click" Margin="0,0,28,0" Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>
    </Grid>
</Window>
