<Window x:Class="FocusDeck.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="FocusDeck"
        Width="1536"
        Height="1024"
        MinWidth="1228"
        MinHeight="818"
        Background="#070B12"
        WindowStyle="None"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei UI">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="PageBrush" Color="#070B12" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111820" />
        <SolidColorBrush x:Key="CardBrush" Color="#171E27" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#303A49" />
        <SolidColorBrush x:Key="InputBrush" Color="#101621" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#303A49" />
        <SolidColorBrush x:Key="TextBrush" Color="#F7F8FA" />
        <SolidColorBrush x:Key="MutedBrush" Color="#B8C0CC" />

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="#F7F8FA" />
            <Setter Property="Background" Value="#171E27" />
            <Setter Property="BorderBrush" Value="#303A49" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#3B82F6" />
                                <Setter TargetName="Root" Property="Background" Value="#1A2533" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#0F1620" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="Background" Value="#126CE7" />
            <Setter Property="BorderBrush" Value="#238BFF" />
        </Style>

        <Style x:Key="ChromeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="52" />
            <Setter Property="Height" Value="52" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="#F7F8FA" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#141C27" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#F7F8FA" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,11" />
            <Setter Property="MinHeight" Value="52" />
            <Setter Property="FontSize" Value="21" />
            <Setter Property="CaretBrush" Value="#F7F8FA" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#2F8DFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="#F7F8FA" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="18,11,40,11" />
            <Setter Property="MinHeight" Value="52" />
            <Setter Property="FontSize" Value="21" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" />
                            <ContentPresenter x:Name="ContentSite"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              TextElement.Foreground="#F7F8FA" />
                            <ToggleButton Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid Background="Transparent">
                                            <Path Data="M 0 0 L 6 5 L 12 0 Z"
                                                  Fill="#B8C0CC"
                                                  Width="12"
                                                  Height="5"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,18,0" />
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Visibility="Hidden"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="#F7F8FA"
                                     CaretBrush="#F7F8FA"
                                     Margin="{TemplateBinding Padding}"
                                     VerticalContentAlignment="Center" />
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#111820"
                                        BorderBrush="#2F8DFF"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer MaxHeight="280">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#2F8DFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#F7F8FA" />
            <Setter Property="Background" Value="#111820" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#15345E" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#126CE7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Width" Value="78" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" Background="#222933" BorderBrush="#2E3947" BorderThickness="1" CornerRadius="20" />
                            <Border x:Name="Thumb" Width="27" Height="27" CornerRadius="14" Background="#DDE7F7" HorizontalAlignment="Left" Margin="6,0,0,0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#126CE7" />
                                <Setter TargetName="Track" Property="BorderBrush" Value="#238BFF" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,6,0" />
                                <Setter TargetName="Thumb" Property="Background" Value="#FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#070B12" BorderBrush="#2B3542" BorderThickness="1" CornerRadius="12" ClipToBounds="True">
            <Canvas x:Name="DesignCanvas" Width="1536" Height="1024" Background="#070B12">
            <Rectangle Width="1536" Height="80" Fill="#090E15" />
            <Line X1="0" X2="1536" Y1="80" Y2="80" Stroke="#1E2835" StrokeThickness="1" />
            <Grid Canvas.Left="0" Canvas.Top="0" Width="1536" Height="80" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" />

            <Grid Canvas.Left="32" Canvas.Top="0" Width="600" Height="80" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="Assets/ui/app-icon-titlebar.png" Width="36" Height="36" Margin="0,0,18,0" />
                    <TextBlock Text="FocusDeck" FontSize="24" FontWeight="SemiBold" Foreground="#F7F8FA" VerticalAlignment="Center" Margin="0,0,20,0" />
                    <TextBlock Text="程序快捷唤起" FontSize="19" Foreground="#B8C0CC" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
            <StackPanel Canvas.Left="1348" Canvas.Top="16" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource ChromeButtonStyle}" Content="-" Click="Minimize_Click" />
                <Button Style="{StaticResource ChromeButtonStyle}" Content="□" Click="Maximize_Click" />
                <Button Style="{StaticResource ChromeButtonStyle}" Content="×" Click="Close_Click" />
            </StackPanel>

            <Image Source="Assets/ui/hero-banner.png" Canvas.Left="48" Canvas.Top="105" Width="1440" Height="233" Stretch="Fill" />

            <Border Canvas.Left="48" Canvas.Top="355" Width="1440" Height="117" Background="#D9111820" BorderBrush="#303A49" BorderThickness="1" CornerRadius="12">
                <Grid>
                    <Image Source="Assets/ui/startup-icon-card.png" Width="72" Height="72" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="34,0,0,0" />
                    <StackPanel Margin="145,29,0,0">
                        <TextBlock Text="开机自动启动" Foreground="#F7F8FA" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock Text="Windows 登录后在后台启动，可随时使用配置的快捷键。" Foreground="#B8C0CC" FontSize="19" Margin="0,8,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,58,0">
                        <Border Background="#331A2430" CornerRadius="18" Padding="18,6" Margin="0,0,18,0">
                            <TextBlock x:Name="StartupStateText" Text="已关闭" Foreground="#B8C0CC" FontSize="17" VerticalAlignment="Center" />
                        </Border>
                        <ToggleButton x:Name="StartupToggle" Checked="StartupToggle_Changed" Unchecked="StartupToggle_Changed" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Canvas.Left="48" Canvas.Top="489" Width="1440" Height="419" Background="#D9111820" BorderBrush="#303A49" BorderThickness="1" CornerRadius="12">
                <Grid>
                    <Image Source="Assets/ui/shortcut-icon-card.png" Width="72" Height="72" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="34,20,0,0" />
                    <StackPanel Margin="145,27,0,0">
                        <TextBlock Text="程序唤起快捷键" Foreground="#F7F8FA" FontSize="24" FontWeight="SemiBold" />
                        <TextBlock Text="按下快捷键切换到程序窗口；窗口已在前台时再次按下会最小化。" Foreground="#B8C0CC" FontSize="19" Margin="0,8,0,0" />
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,28,48,0" Width="158" Height="55" Content="添加程序    +" Click="AddApplication_Click" Style="{StaticResource PrimaryButtonStyle}" />
                    <StackPanel x:Name="ApplicationsPanel" Margin="36,106,42,0" />
                </Grid>
            </Border>

            <Rectangle Canvas.Left="0" Canvas.Top="926" Width="1536" Height="98" Fill="#070B12" />
            <StackPanel Orientation="Horizontal" Canvas.Left="48" Canvas.Top="958">
                <Image Source="Assets/ui/success-status-icon.png" Width="32" Height="32" Margin="0,0,18,0" />
                <TextBlock x:Name="NoticeText" Text="设置已保存，快捷键已生效。" VerticalAlignment="Center" Foreground="#73E070" FontSize="18" />
            </StackPanel>
            <Button Canvas.Left="1112" Canvas.Top="946" Width="132" Height="52" Content="取消修改" Click="Reset_Click" />
            <Button Canvas.Left="1300" Canvas.Top="946" Width="146" Height="52" Content="保存设置" Click="Save_Click" Style="{StaticResource PrimaryButtonStyle}" />
        </Canvas>
    </Border>
</Window>
